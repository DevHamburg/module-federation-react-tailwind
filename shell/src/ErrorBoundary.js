import React from "react";

function ErrorBoundary() {
  return (
    <div>
      <h1>MicroFrontend's could not be loaded. Check if every MicroFrontend is running...</h1>
    </div>
  );
}

export default ErrorBoundary;
